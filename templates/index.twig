{% extends "base.twig" %}

{% block content %}
	{% if (fn('is_home')) %}
		{% include 'html-body-body.twig' %}
	{% endif %}


	{#% for post in posts %#}
		{#% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %#}
	{#% endfor %#}
{% endblock %}
